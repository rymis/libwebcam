INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}")

ADD_SUBDIRECTORY(mihl)

INCLUDE_DIRECTORIES(mihl)

ADD_EXECUTABLE(wwwcam wwwcam.c)

TARGET_LINK_LIBRARIES(wwwcam webcam ${LIBWEBCAM_LIBS} mihl ${LIBJPEG})
